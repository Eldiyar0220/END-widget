import 'package:flutter/material.dart';

class Screen914 extends StatelessWidget {
  const Screen914({super.key});
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Screen914'),
      ),
      body: Column(
        children: [
          Container(),
        ],
      ),
    );
  }
}
import 'package:flutter/material.dart';

class Screen9142 extends StatelessWidget {
  const Screen9142({super.key});
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Screen9142'),
      ),
      body: Column(
        children: [
          Container(),
        ],
      ),
    );
  }
}